#include <stdio.h>
int main (){
    int x,y,sum;
    printf("enter two numbers :");
    scanf("%d %d",&x,&y);
    sum = x+y;
    printf("The sum of the numbers %d + %d = %d\n",x,y,sum);
    return 0;
}