#include <stdio.h>
int main (){
    int i,a,b,sum;
    printf("Enter two numbers :\n");
    scanf("%d %d",&a,&b);
    sum = a+b;
    i = sum;
    printf("The sum of the numbers %d & %d is %d \n",a,b,i);
    return 0;
}   